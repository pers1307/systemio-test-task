services:
    sio_test:
        user: app
        build:
            context: .
            args:
                USER_ID: ${USER_ID}
        ports:
            - "8337:8337"
        volumes:
            - .:/app:rw
            - ${HOME}/.composer:/home/app/.composer
        environment:
            - APP_ENV=dev
